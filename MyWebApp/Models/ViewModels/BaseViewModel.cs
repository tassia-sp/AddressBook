using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace MyWebApp.Models.ViewModels
{
    /// <summary>
    /// right now this class doesn't do anything. Eventually when user log in is 
    /// added, the current logged in user's info will be accessed through this class in the layout view.
    /// </summary>
    public class BaseViewModel
    {
    }
}